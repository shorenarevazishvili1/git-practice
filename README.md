# TBC QA 2026

This is a simple Maven-based Java project created as part of Git and GitHub practice.

## 📋 Project Overview

The project contains:
- ☕ A Java class named `HelloWorld`
- 📦 Maven project structure with `pom.xml`
- 🔄 Git version control with proper branching strategy
- 🚫 A `.gitignore` file for excluding IDE and build files
- ✅ Ready for unit testing with JUnit


The application prints to the console:
```java
System.out.println("Hello, Git!");